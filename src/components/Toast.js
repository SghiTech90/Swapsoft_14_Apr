import Toast from "react-native-simple-toast"; // kept to avoid import errors

// Toast notifications are temporarily disabled.
// To re-enable, uncomment the line below inside the Toaster function.
export async function Toaster(msg) {
  // Toast.show(msg, Toast.SHORT);
}
